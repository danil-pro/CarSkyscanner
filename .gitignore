# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Node / Next
node_modules/
.next/
out/
frontend/.env.local

# Playwright / secrets
storage_state.json
*.storage_state.json
.env
backend/secrets/*
!backend/secrets/.gitkeep

# OS
.DS_Store

# Editor
.idea/
.vscode/

# Claude Code (local tooling/state) — not part of the project
.claude/

# Stray root lockfile (no root package.json; the real one is frontend/package-lock.json)
/package-lock.json
